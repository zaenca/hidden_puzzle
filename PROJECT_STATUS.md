# PROJECT STATUS

Гибрид puzzle + hidden object. Vertical slice: площадь → пекарня.
Архитектура: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

**Обновлено:** 2026-08-24
**Текущий этап:** M1 завершён; фасад пекарни переведён на point-and-click

---

## ⚠ Изменение курса: фасад вместо списка задач (2026-08-24)

Цепочка из пяти задач на фасаде («расчистить вход» → «вывеска» → «замок» →
«витрина») **убрана из контента**. Вместо неё игрок работает прямо с объектами:
тап по двери → находится ключ → ключ выбирается в сумке → тап по двери им же →
дверь открыта. Правила тапа лежат в `content/shops/*.json` (`interactions`),
исполняются в `MetaService.interact()`, сцена про ключи и двери не знает.

Что это означает для остального:

| Было | Стало |
|---|---|
| `tasks.json` — 5 задач | 1: `task_survey_district` (карта, открывает пекарню) |
| `actions.json` — 5 действий | 1: `choose_bakery` |
| `level_index.json` — уровни 1–5 | только `bakery_01` |
| `content/levels/bakery_02..05.json` | **файлы на месте, но не в индексе** — движок их не видит; вернуть = добавить строку в индекс и владельца-задачу |
| фасад: список из 4 задач | один пункт «Войти в пекарню» (открывается по флагу `door_open`) |
| cooldown ремонта замка | в слайсе сейчас не используется (сам `CooldownService` цел) |

Дефолты слотов пекарни выставлены в «фасад уже приведён в порядок»: вывеска
висит, витрина чистая, мусора нет — их задачи убраны, и оставлять их в
разрушенном виде было бы тупиком. Единственный незакрытый объект — дверь.
**Старый сейв этого не знает: нужен «Сброс прогресса» в debug-панели.**

---

## Как запустить

Движок в проекте не лежит. Проверено на **Godot 4.6.2 stable** (M1) и на
**Godot 4.7.2 stable** (M1.1, `D:\Godot\Godot_v4.7.2-stable_win64.exe`).
`config/features` в `project.godot` всё ещё `"4.6"` — 4.7.2 это не мешает.

Игра:

```bash
godot --path . 
```

Автопрогон всего слайса без участия игрока (headless, ~25 c):

```bash
godot --headless --path . --autoplay
```

Отчёт пишется построчно в `autoplay_report.txt` в корне проекта.

Валидатор контента:

```bash
godot --headless --path . --script res://tools/validate_content.gd
```

Первый запуск на новой машине требует импорта: `godot --headless --path . --import`.

**В игре:** `⚙` в правом верхнем углу — debug-панель (сброс прогресса, +гемы,
+бустеры, перемотка времени на 10 минут). Внутри уровня: `S` — собрать пазл,
`F` — найти все предметы.

---

## Что уже работает

### Полный цикл (проверяется автоматически, 37 проверок)

```
карта площади → уровень 1 → jigsaw → бесшовное раскрытие → hidden object
  → мета: пекарня выбрана (сумка после разговора с мэром остаётся пустой)
  → карта: закрытые здания (в т.ч. мэрия) кликабельны и отвечают тапом
  → пекарня (реальный арт фасада) → тап по запертой двери → ключ в сумке
  → ключ выбран в сумке → тап по двери → дверь открыта, ключ израсходован
  → флаг door_open открывает задачу «Разобрать завал в зале»
  → уровень 2: jigsaw по чистому интерьеру → раскрытие → проявляется слой
    предметов → поиск метлы и мешка → мета: зал убран
  → сохранение → перезагрузка → все состояния на месте
```

Прогон проверяет и негативные случаи: второй тап не выдаёт второй ключ,
чужой предмет дверь не открывает, открытая дверь ключ не возвращает.

### Системы

| Система | Состояние |
|---|---|
| `Game` — FSM приложения, маршрутизация, связка core ↔ мета | готово |
| `ContentDB` — JSON-контент, лёгкий индекс, ленивая загрузка уровней, LRU-кэш | готово |
| `PlayerState` — кошелёк, инвентарь, бустеры | готово |
| `MetaService` + `TaskResolver` + `EffectRunner` | готово |
| `CooldownService` — абсолютный `ends_at`, offline, сокращение, guard отката часов | готово |
| `SaveService` — JSON, атомарная запись, `.bak`, версия, каркас миграций | готово |
| `MockServices` — rewarded ad и IAP без SDK | готово |
| `HybridLevel` — один контроллер на все уровни, FSM из 6 фаз | готово |
| `JigsawModule` — генерация частей кодом, drag&drop, лоток, снап, бустер | готово |
| `HiddenObjectPhase` — полигональные цели, прощение промаха, подсветка | готово |
| `SceneView` — общий визуал обеих фаз, вписывание арта, dim/reveal, слой предметов | готово |
| `PlaceholderArt` — генерация фонов и иконок; финальный арт перекрывает генерацию | готово |
| `ShopScene` + `StateSlot` — визуальные состояния, сумка, тап по объектам | готово |
| `SlotInteraction` + `MetaService.interact()` — point-and-click из данных | готово |
| `MapScene` — здания, состояния, задачи локации `map` | готово |
| `validate_content.gd` — 20+ проверок, включая симуляцию прогресса | готово |
| `AutoplayDriver` — сквозной headless-прогон | готово |

### Контент

2 активных уровня (`bakery_01`, `bakery_02`; файлы 03–05 лежат вне индекса),
42 предмета, 2 задачи, 2 meta action, 1 магазин с 5 слотами визуальных
состояний и 4 правилами тапа на двери.

Финальный арт: `map_city.jpg` (карта), `bakery.png` (фасад),
`bakery_interior.png` + `bakery_interior_objects.png` (торговый зал).
**Ни одного скрипта под конкретный уровень** — добавление уровня это один JSON.
Ни одного скрипта под конкретную дверь: правило тапа — это тоже JSON.

### Визуальные изменения в мете за слайс

| После чего | Что меняется |
|---|---|
| Уровень 1 + действие | пекарня становится доступной на карте |
| Тап по запертой двери | в сумке появляется ключ, флаг `bakery_key_found` |
| Тап по двери с ключом в руке | дверь открыта, ключ израсходован, флаг `door_open` |
| Флаг `door_open` | открывается задача «Разобрать завал в зале» |
| Уровень 2 + действие | метла и мешок израсходованы, флаг `hall_cleared` |

---

## Ключевые технические решения этого этапа

**Части пазла генерируются кодом.** `JigsawGeometry` строит сетку замков
(Catmull-Rom по профилю), `Polygon2D` берёт UV с общей текстуры фона.
Соседние части используют один массив точек общей грани, поэтому замки всегда
совпадают. Стоимость производства пазла для нового уровня — ноль.

**Бесшовность настоящая, а не имитация.** Фон, слои и цели живут в `SceneView`
с самого начала. Пазл рисуется поверх, фон приглушён. При решении: гаснут швы →
гаснет слой пазла → под ним пиксель-в-пиксель то же изображение → лёгкий
push-in камеры → цели активны. Смены сцены нет.

**Предметы запечены в фон.** `PlaceholderArt` рисует цели прямо в изображение
уровня — так же, как это будет с финальным PNG. Поиск идёт по цвету и форме,
которые продублированы в панели искомых предметов, а не по невидимым хитбоксам.

**Контент — JSON, сейв — JSON, runtime-контракт — типизированные Resource.**
Отклонение от плана (там были `.tres`): вложенные sub_resource неудобно писать
и мержить вне редактора. Классы остались `Resource` с `@export`, поэтому
переход на `.tres` затронет только `ContentDB`.

**Core физически не знает мету.** В `res://core/**` нет ни одного обращения к
`PlayerState`, `MetaService`, `CooldownService` и ни одного упоминания пекарни.
Уровень получает `LevelContext`, отдаёт `LevelResult`.

---

## Что решено по вопросам из плана

1. Провала уровня нет, таймера нет, lives нет — согласовано.
2. Повтор пройденных уровней разрешён: награда × 0.3, сюжетные предметы
   повторно **не** выдаются (`MetaService.apply_level_result`).
3. Первый cooldown перенесён в слайс: после уровня 4 запускается ремонт замка
   входной двери (10 мин), уровень 5 идёт параллельно и сокращает ожидание на
   3 минуты; мгновенное завершение — 25 mock-гемов, реклама — −2 минуты.
4. Финальный арт подключается через данные (`art.background`,
   `art.objects_background`, `background` у магазина). Где файла нет, визуал
   по-прежнему генерируется процедурно — проект запускается без ассетов.

---

## Текущие ограничения

| Ограничение | Комментарий |
|---|---|
| Godot 4.6.2, а не 4.7.2 | 4.7.2 не установлен. Проверено на 4.6.2; для перехода поменять `config/features` в `project.godot` на `"4.7"` |
| Арт есть у карты, фасада и зала | уровень 1 (`bakery_01`) по-прежнему на процедурном фоне: цели в нём — цветные формы |
| Камера в уровне зафиксирована | pan/zoom и pinch в HO-фазе не реализованы (`allow_zoom` в данных уже есть) |
| Нет mid-level resume | выход из уровня теряет прогресс уровня; контракт `PuzzleModule.serialize_state()` под это уже заложен |
| Нет разметчика целей | цели пишутся руками в JSON; `tools/ho_marker_tool` — следующий шаг (M6) |
| Один puzzle-модуль | jigsaw; реестр `PuzzleRegistry` готов принять screws/bubbles |
| Один бустер | универсальный: в jigsaw ставит часть, в HO подсвечивает предмет |
| Safe area не проверена на устройстве | код есть (`ui/safe_area.gd`), на десктопе отступы нулевые |
| Нет Android-сборки | export-пресет не настроен (M7) |
| `boot_log.txt` / `autoplay_report.txt` | отладочные файлы в корне, пишутся только из dev-запуска |
| Миграций сейва пока нет | версия 1, каркас `SaveService.MIGRATIONS` пустой |
| Звука нет | сознательно вне слайса |

---

## Журнал этапов

### M0 — Скелет проекта · ✅

- `project.godot` (portrait 1080×1920, `canvas_items`/`expand`, GL Compatibility,
  emulate_touch_from_mouse).
- 8 автолоадов, каталоги `core/ meta/ data/ ui/ content/ tools/`.
- `Boot` + `Game.goto()` как единственная точка смены сцен, debug-оверлей.

**Проверка:** проект импортируется без ошибок, приложение стартует на карту района.

### M1 — Полный цикл · ✅

- jigsaw с генерацией геометрии, drag&drop, лотком и снапом;
- бесшовный reveal;
- hidden object с полигональными целями и выдачей сюжетных предметов;
- мета: задачи, meta actions, эффекты, визуальные состояния;
- cooldown с сокращением за уровни и mock-ускорением;
- локальное сохранение и восстановление.

Дополнительно (обгоняя план, потому что без этого нельзя было проверять):
data-driven контент (изначально M2), сохранение (M3), мета-слой (M4),
cooldown (M5) — в минимальном объёме.

**Проверка:**

| Прогон | Результат |
|---|---|
| `--headless --autoplay` | 37 проверок, провалено 0, exit 0 |
| `--autoplay` в окне (реальный OpenGL) | тот же результат, exit 0 |
| `--quit-after 600` в окне (просто постоять на карте) | ни одной ошибки в логе |
| `validate_content.gd` | 0 ошибок, 0 предупреждений |

**Два оставшихся warning'а (оба ожидаемые, не баги):**

- `во время cooldown из активностей остался только повтор уровней` — срабатывает
  в самом конце слайса, когда все пять задач закрыты, а последний cooldown ещё
  идёт. Это не дефект кода, а граница контента: новых задач за уровнем 5 пока
  нет. Инвариант специально отличает «остался только повтор» от «нечем заняться
  совсем» — второе было бы настоящей ошибкой.
- `RIDs / ObjectDB leaked at exit` — **открытый вопрос, причина не установлена.**
  Наблюдения: при запуске, где игрок только постоял на карте (`--quit-after 600`),
  предупреждения нет; после прохождения уровней оно есть — и при выходе через
  `SceneTree.quit()` в автопрогоне, и при обычном закрытии окна (11 и 14
  CanvasItem соответственно). То есть течёт что-то на пути уровня, а не артефакт
  порядка выключения теста. Подозрения (не проверены): узлы-маркеры найденных
  целей в `SceneView.markers` и удерживающие их твины в
  `HiddenObjectPhase._draw_found_marker`, либо твины частей пазла в
  `JigsawPiece.send_to_tray`. На геймплей не влияет, но разобраться нужно в M2 —
  запустить с `--verbose` и посмотреть, какие именно объекты остаются.

**Найдено и починено на этом этапе:**
- `PlayerState._set` перекрывал виртуальный `Object._set` → переименован в `_write`;
- `AutoplayDriver` как RefCounted освобождался сразу после вызова, корутина
  не возобновлялась → ссылка удерживается в `Boot`;
- `ShopScene._update_timer` вызывал `call_deferred("_rebuild_tasks")`, который
  снова вызывал `_update_timer` → бесконечная очередь deferred внутри одного
  кадра и полное зависание. Перестроение теперь только по
  `EventBus.cooldown_finished` + флаг `_rebuilding`.

### M1.1 — фасад как point-and-click · ✅

Список задач на фасаде заменён прямым взаимодействием с объектами
(подробности — в разделе «Изменение курса» вверху).

- `SlotInteraction` — правило тапа как данные: при каком состоянии слота,
  каким предметом, что выдать/списать, куда перевести слот, какой флаг поднять.
- `MetaService.interact(shop_id, slot_id, selected_item)` — единственное место,
  где тап превращается в изменение мира. Сцена и headless-прогон ходят сюда же.
- `ShopScene` — хит-тест по слотам, сумка с выбором предмета «в руку»,
  подсветка объектов, с которыми сейчас есть что делать.
- `MapScene` — здание без `shop_id` (мэрия) больше не декорация: попадает в
  хит-тест и честно отвечает «Объект пока закрыт».
- Валидатор дополнен: состояния и предметы в правилах тапа, достижимость флага
  входа, «выдаётся, но никем не тратится» вместо старого «quest без action».

**Проверка:**

| Прогон | Результат |
|---|---|
| `--headless --autoplay` | 29 проверок, провалено 0, exit 0 |
| `validate_content.gd` | 0 ошибок, 0 предупреждений |
| окно, реальный OpenGL (Godot 4.7.2) | сцены строятся без ошибок, весь путь двери пройден вручную |

### M1.2 — финальный арт и торговый зал · ✅

Уровень перестал быть процедурным прямоугольником: арт приходит из данных,
а фаза поиска получила собственный слой изображения.

- `SceneArt.objects_background` — второй слой кадра. Пазл собирается по
  **чистой** комнате, предметы поиска проявляются только на reveal. Раньше
  предметы были запечены в ту же картинку, и их было видно по частям в лотке.
- `SceneView` вписывает арт в кадр по пропорциям (`_fit`) и отдаёт пазлу
  `uv_scale`. Это же чинит старое расхождение: `reference_size` 1080×1350
  растягивался в кадр 1080×1300, и части пазла не совпадали с фоном.
- Глубина `set_dim` зависит от наличия слоя предметов: прятать в фоне больше
  нечего, и на тёмном арте сильное затемнение только мешало собирать.
- `ShopDefinition.background` — фасад тоже из данных. Состояния, которые уже
  нарисованы на арте, помечены `hidden`, поверх рисуются только «сломанные».
- Осмотр района больше не выдаёт «План района» в сумку: `choose_bakery` теперь
  требует пройденный уровень (`type: level`), а не предмет. Разговор с мэром —
  это разговор, а не добыча.
- Валидатор научился симулировать point-and-click: без этого всё, что живёт за
  дверью, открываемой тапом, считалось недостижимым.
- `debug_autoplay` ждёт фазу, а не фиксированные секунды: длительность reveal
  теперь зависит от контента.

**Уровень 2 «Торговый зал»** — jigsaw 3×4 по `bakery_interior.png`, затем
ровно две цели: метла и мешок для мусора на `bakery_interior_objects.png`
(`required_normal: 0`).

**Проверка:**

| Прогон | Результат |
|---|---|
| `--headless --autoplay` | 37 проверок, провалено 0, exit 0 |
| `validate_content.gd` | 0 ошибок, 0 предупреждений |
| окно, реальный OpenGL (Godot 4.7.2) | фасад, пазл, стык частей, reveal и обе цели сняты и проверены |

---

## Следующий шаг

**M2 — играбельность на реальном вводе.** Автопрогон доказывает логику, но не
ощущение. Порядок:

1. Прогнать слайс руками в окне: проверить попадание по частям пазла пальцем
   и мышью, читаемость лотка, ощущение reveal.
2. По результатам — подстроить `snap_distance_px`, `tray_scale`, `LIFT`
   и длительности reveal.
3. Pan/zoom камеры в HO-фазе (`HOConfig.allow_zoom` уже в данных).
4. Разметчик целей `tools/ho_marker_tool` — до того, как уровней станет больше пяти.

Дальше по плану: M6 (контент + инструменты), M7 (mobile polish + Android export).
